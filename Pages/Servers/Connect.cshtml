@page "/servers/{ServerIdentifier}"
@model ASP_site.Pages.Servers.ConnectModel
@{
    // This content will likely not be displayed because OnGet directly redirects.
    // It serves as a placeholder and defines the route and model.
    ViewData["Title"] = "Connecting to Server";
} 